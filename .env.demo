# Modo demo: la app corre SIN Firebase, con datos de ejemplo en memoria.
# Lo carga Vite al usar `npm run dev:demo`. No contiene secretos: es seguro
# tenerlo en el repo. No se necesitan las credenciales VITE_FIREBASE_*.
VITE_DEMO_MODE=true
